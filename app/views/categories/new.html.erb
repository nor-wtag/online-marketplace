<h1><%= t('categories.create_new') %></h1>

<% if can?(:create, Category) %>
  <%= render 'form', category: @category %>
<% else %>
  <p><%= t('categories.unauthorized_create') %></p>
<% end %>

<%= link_to t('categories.back_to_categories'), categories_path, class: 'btn btn-secondary' %>
