<h1><%= t('categories.all') %></h1>

<% if can?(:create, Category) %>
  <%= link_to t('categories.new_category'), new_category_path, class: 'btn btn-primary' %>
<% end %>

<%= render 'categories_table' %>
<%= link_to t('categories.back_to_homepage'), homepage_path, class: 'btn btn-secondary' %>
