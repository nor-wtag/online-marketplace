<h1 class="mb-3"><%= t('categories.all') %></h1>

<% if can?(:create, Category) %>
  <%= link_to t('categories.new_category'), new_category_path, class: 'btn btn-primary mb-3' %>
<% end %>

<%= render 'categories_table' %>
<%= link_to t('categories.back_to_homepage'), homepage_path, class: 'btn btn-secondary mt-3' %>
