<h1 class="text-center mt-4"><%= t('users.index.welcome_title') %></h1>

<div class="d-flex justify-content-center">
  <div class="m-3">
    <h2 class="text-center"><%= t('users.index.sign_up_title') %></h2>
    <%= link_to t('users.index.sign_up_link'), new_user_registration_path, class: 'btn btn-primary btn-lg w-100' %>
  </div>
  <div class="m-3">
    <h2 class="text-center"><%= t('users.index.sign_in_title') %></h2>
    <%= link_to t('users.index.sign_in_link'), new_user_session_path, class: 'btn btn-secondary btn-lg w-100' %>
  </div>
</div>
