<h1><%= t('products.new.title') %></h1>

<% if can?(:create, Product) %>
  <%= render 'form', product: @product %>
<% else %>
  <p><%= t('products.new.unauthorized_message') %></p>
<% end %>

<%= link_to t('products.new.back_to_products'), products_path %>
